package spark.jobserver.util

import org.scalatest.{FunSpecLike, Matchers}
import scala.collection.mutable.HashMap

class LauncherSpec extends FunSpecLike with Matchers {
//  describe("Tests") {
//    it("should not start if current directory is not valid") {
//      val envMap = HashMap(("appdir", "/tmp"), ("MANAGER_JAR_FILE", ""))
//      val launcher = new ManagerLauncher(null, null, "", "", "") {
//        override def getEnvironmentVariable(name: String): String = {
//              
//        }
//      }
//      
//    }
//  }
}